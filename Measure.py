import abc
import numpy as np

class Measure:
    """Array of supports (elements of M) and values (unsigned double).
    Parameters
    ----------
    """

    def __init__(self,support,masses):
        self.supports = supports
        self.masses = masses
        return

    

